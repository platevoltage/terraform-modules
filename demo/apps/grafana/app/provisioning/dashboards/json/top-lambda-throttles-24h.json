{
  "id": null,
  "uid": "top-lambda-throttles-24h",
  "title": "Top Lambda Throttles - 24h",
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "editable": true,
  "tags": ["lambda", "prometheus"],
  "time": { "from": "now-24h", "to": "now" },
  "refresh": "1m",
  "panels": [
    {
      "id": 1,
      "type": "barchart",
      "title": "Top 10 by Throttles (24h)",
      "gridPos": { "h": 12, "w": 24, "x": 0, "y": 0 },
      "targets": [
        {
          "refId": "A",
          "editorMode": "code",
          "expr": "topk(10, sum by (function_name) (sum_over_time(aws_lambda_throttles_sum{job=\"cloudwatch_exporter\", exported_job=\"aws_lambda\"}[24h])))",
          "legendFormat": "{{function_name}}",
          "range": true
        }
      ],
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "options": {
        "legend": { "displayMode": "table", "placement": "right", "showLegend": true },
        "orientation": "horizontal",
        "barWidth": 0.8,
        "tooltip": { "mode": "single" },
        "stacking": "none"
      },
      "transformations": [
        { "id": "organize", "options": { "renameByName": { "Value": "throttles" } } }
      ]
    }
  ]
}
